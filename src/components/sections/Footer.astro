---
import siteSettings from '../../config/site_settings.json';
import SocialIcons from '../ui/SocialIcons.astro';

const currentYear = new Date().getFullYear();
const { brand, contact } = siteSettings;
---

<footer class="bg-white border-t border-gray-100 pt-16 pb-8">
  <div class="container mx-auto px-6">
    
    <div class="grid grid-cols-1 md:grid-cols-4 gap-12 mb-12">
      
      <div class="col-span-1 md:col-span-2">
        <a href="/Aimentool/" class="text-2xl font-bold text-[var(--primary)] tracking-tighter mb-4 block">
            {brand.name}
        </a>
        <p class="text-gray-500 mb-6 max-w-sm">
          {brand.description}
        </p>
        <SocialIcons />
      </div>

      <div>
        <h4 class="font-bold text-gray-900 mb-4">Menü</h4>
        <ul class="space-y-2">
          <li><a href="/Aimentool/" class="text-gray-600 hover:text-[var(--primary)] transition-colors">Kezdőlap</a></li>
          <li><a href="/Aimentool/#features" class="text-gray-600 hover:text-[var(--primary)] transition-colors">Szolgáltatások</a></li>
          <li><a href="/Aimentool/contact" class="text-gray-600 hover:text-[var(--primary)] transition-colors">Kapcsolat</a></li>
        </ul>
      </div>

      <div>
        <h4 class="font-bold text-gray-900 mb-4">Elérhetőség</h4>
        <ul class="space-y-3 text-gray-600">
          <li class="flex items-start gap-3">
             <span class="text-[var(--primary)] mt-1">📍</span>
             {contact.address}
          </li>
          <li class="flex items-center gap-3">
             <span class="text-[var(--primary)]">📧</span>
             <a href={`mailto:${contact.email}`} class="hover:text-[var(--primary)]">{contact.email}</a>
          </li>
          <li class="flex items-center gap-3">
             <span class="text-[var(--primary)]">📞</span>
             <a href={`tel:${contact.phone}`} class="hover:text-[var(--primary)]">{contact.phone}</a>
          </li>
        </ul>
      </div>

    </div>

    <div class="border-t border-gray-100 pt-8 flex flex-col md:flex-row justify-between items-center gap-4 text-sm text-gray-500">
      <p>&copy; {currentYear} {brand.name}. Minden jog fenntartva.</p>
      <div class="flex gap-6">
        <a href="#" class="hover:text-gray-900">Adatvédelem</a>
        <a href="#" class="hover:text-gray-900">ÁSZF</a>
      </div>
    </div>

  </div>
</footer>